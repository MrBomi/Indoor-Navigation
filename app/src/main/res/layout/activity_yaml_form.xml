<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:id="@+id/formContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <EditText
            android:id="@+id/precision"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Precision (e.g. 0.1)"
            android:inputType="numberDecimal" />

        <EditText
            android:id="@+id/wallLayer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Wall Layers (comma-separated)"
            android:inputType="text" />

        <EditText
            android:id="@+id/doorLayer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Door Layers (comma-separated)"
            android:inputType="text" />

        <EditText
            android:id="@+id/roofLayer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Roof Layers (comma-separated)"
            android:inputType="text" />

        <Button
            android:id="@+id/submitYamlBtn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Submit YAML and Upload" />
    </LinearLayout>
</ScrollView>
